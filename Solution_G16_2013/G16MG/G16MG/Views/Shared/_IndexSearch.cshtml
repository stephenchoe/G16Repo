@model G16_2013.Models.MemberModel.SearchInputModel


@*@using (Html.BeginForm(Model.ActionName, Model.ControllerName, FormMethod.Post, new { @class = "form-horizontal", role = "search" }))
{*@
    <div class="col-md-4">
        <div class="form-group">
            <label for="searchway" class="col-md-6 control-label text-muted">條件搜尋</label>
            <div class="col-sm-6">
                @Html.DropDownListFor(m => m.SearchWayId, new SelectList(Model.SearchOptions, "Value", "Text", Model.SearchWayId), new { @class = "form-control ", @id = "searchway", onchange = "$(this.form).submit();" })

            </div>
        </div>
    </div>
    @* 搜尋 *@
    <div class="col-md-4">
        <div class="row">
            <div class="col-md-8" style="text-align: right">
                <label class="sr-only">搜尋關鍵字</label>
                @Html.TextBoxFor(m => m.KeyWord, new { @class = "form-control ", @placeholder = "請輸入搜尋關鍵字" })

            </div>
            <div class="col-md-4">
                <button type="submit" class="btn btn-default ">搜尋</button>
            </div>
        </div>

        <div class="form-group">
        </div>
    </div>



@*}*@
